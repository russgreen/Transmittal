<Window x:Class="Transmittal.Views.StatusView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Transmittal.ViewModels" 
        xmlns:local="clr-namespace:Transmittal.Views"
        mc:Ignorable="d"
        Title="Sheet Status" Width="375" 
        SizeToContent="Height"
        WindowStyle="SingleBorderWindow" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterOwner">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="AppStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Style>
        <StaticResource ResourceKey="WindowStyle" />
    </Window.Style>

    <StackPanel Margin="10" Orientation="Vertical" >
        <!--<TextBlock Text="Sheet Status" FontWeight="Bold" FontSize="14" />-->
        <ComboBox ItemsSource="{Binding DocumentStatuses}"
            DisplayMemberPath="DisplayName"
            SelectedValuePath="Code"
            SelectedItem="{Binding SelectedDocumentStatus}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0" Height="30" >
            <Button Content="Set Status" Command="{Binding SendStatusCommand}" IsDefault="True" />
            <Button Content="Cancel" Margin="5,0,0,0" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
